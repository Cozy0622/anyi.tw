- 階段: 開始大改動 (本地開發與測試) - 2026-03-04
- 限制: 僅本地開發與預覽，禁止推送到遠端直到指令下達。
- 本地分支: local/major-update

- 2026-03-04: 完成首頁 WebGL 視覺版重構，包含 4 欄位切片與流體背景。
- 2026-03-05 (全站視覺與數據大改動):
  - 全站風格統一：將底色從純黑提升至「高級深灰 (#222222)」，並將 WebGL 背景亮度提升 30%，找回品牌呼吸感。
  - 建設與無障礙空間分頁：
    - 正式更名並與首頁服務 01 對接。
    - 完整整合「大鎵營造 (UBN 83462457)」超過 30 筆政府標案實績數據。
    - 實作「建築畫廊式」排版，移除壞掉的 3D 動畫，改用專業圖片預留框。
  - 品牌資訊與聯繫優化：
    - 移除右上角與手機選單內之 Inquiry 按鈕，追求極致簡約。
    - 更新社交媒體：指向官方 IG、FB，並換裝 LINE 官方「加入好友」標章。
    - 隱私強化：移除頁尾地址與電話，更新官方聯絡信箱為 `anoacozy@gmail.com`。
    - 聯絡頁面簡約化：僅保留高品質頁尾區塊。
  - 搜尋優化：加入 `icon.jpg` 並設定 Favicon 與 Apple-Touch-Icon，優化 Google 搜尋縮圖。
  - 內容鎖定：嚴格遵循 KISS 原則，還原四大服務完整解析文字，移除所有 Emoji 與廉價裝飾。
  - 備份紀錄：已於大改動前執行 `backups/2026-03-05-major-update/` 全量備份。
  - 目前狀態：已準備好部署至生產環境 https://anyicare.tw/。

- 2026-03-11 (環境診斷與 Navbar 視覺識別重構):
  - 環境診斷：識別並記錄 win32 環境下 node-pty binary 缺失問題，透過 cmd.exe 重新導向成功啟動 Vite 預覽伺服器 (Port 3002)。
  - 資源管理：建立 `assets/images/logo` 並同步至 `public/` 資料夾，確保靜態資源載入路徑正確。
  - Navbar 品牌區塊重構：
    - 實作多次視覺迭代，最終採用單一完整品牌 Logo 圖片 (`1.png`) 替換原本的分離文字結構。
    - 調整 Logo 高度至 `h-24` (96px)，並將導覽列背景完全切齊圖片頂部與底部邊緣 (`py-0`)，追求極致簡潔的視覺。
  - 導覽連結優化：將電腦版導覽文字放大至 `text-base` (16px)，並微調字距 (`tracking-0.1em`) 與加粗，提升可讀性。
  - 手機版功能修復與重構 (重大突破)：
    - **結構重組**：將手機選單 (Mobile Nav) 移出 Header 成為獨立兄弟節點，徹底解決 Stacking Context 導致的捲動破圖 Bug。
    - **圖示升級**：全數替換為原生高品質 SVG 圖示（Menu 三條線、Close X），排除字體載入風險。
    - **視覺質感**：實現 20% 透明度 (`bg-black/20`) 結合超強模糊 (`backdrop-blur-xl`) 的極致毛玻璃效果。
    - **文字微調**：將手機版導覽文字縮小至 `text-2xl`，字距寬鬆度 `0.2em`，提升視覺精緻度。
    - **穩定性**：實作極簡 `overflow: hidden` 滾動鎖定邏輯，確保行動端體驗完美。
  - Git 環境維護：建立 `.gitignore` 排除 `node_modules`、`.vite` 及日誌檔，保持倉庫純淨。
  - 任務日誌更新：完整記錄所有視覺變更與技術診斷過程。

- 2026-03-21 (部落格架構轉型與 AEO/SEO 內容發佈):
  - **系統轉型**：全站將「最新消息」全面更名為「部落格 (Blog)」，並將路由統一優化為小寫路徑 `/blog`。
  - **架構優化 (重大重構)**：
    - 將 `constants.tsx` 中的部落格資料抽離至專屬 `data/blogData.ts`，實現資料與邏輯解耦。
    - 建立 `pages/blog/` 子目錄管理個別文章組件，提升系統擴充性。
    - 建立 `blog_assets/` 資產管理區，實作「日期 > 文檔/圖片」之層級存儲結構。
  - **專業內容發佈 (AEO/SEO 最佳化)**：
    - 發佈首篇專業防禦指南：《密閉空間遭遇辣椒水？長輩第一時間的保命關鍵》。
    - **AEO (AI 引擎最佳化)**：於文章頂部實作結構化「快速解答區」，提升 AI 搜尋引擎 (Google SGE, Perplexity) 引用率。
    - **SEO 佈局**：重塑核心標題，導入「氣膠化學污染」、「呼吸道誘發性痙攣」等專業術語，並實作緩解物資排行表格。
    - **視覺與動畫**：導入 GSAP 實作優雅的入場動畫（標題位移、內容淡入、浮動導航），維持品牌高級感。
  - **導覽與路由**：更新 Navbar 連結與 App 路由配置，確保路徑銜接正確。
  - **部署準備**：完成本地環境驗證，準備推送到生產環境網域 `anyi.tw`。
